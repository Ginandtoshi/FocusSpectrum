# Python packages useful for a desktop prototype
# Note: a browser-based prototype will use JS libs (WebGazer, p5.js/pixi.js, howler.js)

pygame>=2.0.0
numpy>=1.21
opencv-python>=4.5
matplotlib>=3.4
scipy>=1.7
flask>=2.0
mediapipe>=0.8.9
utils
perlin-noise


# if you build a local web UI that proxies eye-tracker data

# Optional (for advanced gaze analysis / hardware SDKs):
# pupil-apriltags (Pupil Labs ecosystem), pylsl (Lab Streaming Layer)

# For production-grade web: manage JS packages with npm/yarn and include webgazer
# in the client code: https://github.com/webgazer/webgazer
